# Collateral Agent secrets. Do not commit real values.
# Mirror this file's contents into GitHub repo secrets for the cron.

# Claude API (content generation)
ANTHROPIC_API_KEY=sk-ant-...

# Wix Data REST API + Wix Media Manager API
WIX_API_KEY=...
WIX_SITE_ID=...

# SMTP (outbound notification email)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=tyler@honeycombcredit.com
SMTP_PASS=...

# Where notification + summary emails are sent
NOTIFY_RECIPIENT=tyler@honeycombcredit.com
