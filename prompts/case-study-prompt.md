# Honeycomb Credit Case Study — Runtime Generation Prompt

> **PLACEHOLDER.** Paste the full runtime prompt here from the project source (the document titled "Honeycomb Credit Case Study — Runtime Generation Prompt", aligned with Collateral Agent spec v3.3). The agent's `generator.ts` reads this file at startup and sends it as the system prompt for every Claude API call.
>
> Until the real prompt is pasted, generation calls will fail validation and the agent will report a `GenerationError` for every campaign. This is intentional — the prompt is the most-tuned artifact in the system and should be moved over verbatim, not approximated.
